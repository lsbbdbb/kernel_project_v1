# Sample CVE list for testing - Kernel CVE Livepatch Auto-Generation Agent
# Each line represents a CVE from a different kernel subsystem
# Usage: ./run --cves sample_cves.txt --workdir ./output

# Real CVEs from upstream Linux kernel
# Data provenance: NVD API 2.0 (metadata) + GitHub torvalds/linux (patches)
# All CVEs have confirmed linux-6.6.y stable backports

# Subsystem: net/sctp (expected: success)
CVE-2025-21638

# Subsystem: include/net/lapb (expected: compile/api_mismatch)
CVE-2024-56659

# Subsystem: drivers/net/ath9k (expected: kpatch_limit/no_fentry)
CVE-2024-53156

# Subsystem: kernel/time (expected: kpatch_limit/struct_or_data_change)
CVE-2025-21767

# Subsystem: fs/btrfs (expected: kpatch_limit/struct_or_data_change)
CVE-2024-46733

# Subsystem: drivers/block/ublk (expected: patch_apply/hunk_failed)
CVE-2024-56764

# Subsystem: drivers/hwmon (expected: compile/missing_api_or_include)
CVE-2025-21656

# Subsystem: kernel/trace (expected: compile/missing_api_or_include)
CVE-2024-56763

# Subsystem: drivers/net/ethernet/ti (expected: kpatch_limit/no_fentry)
CVE-2025-21799

# Subsystem: fs/afs (expected: compile/field_mismatch)
CVE-2025-21646
